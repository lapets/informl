# Module AllConstructsimport UxADTimport Informl

x = 10# Local variable.
y = 20# Local variable.
ifNone:
  return x

elifx != y:
  return UxADT.C("Impossible", [])

else:
  return UxADT.C("What", [])

ps = [1, 2, 3, 4]# Local variable.
z = 0# Local variable.
while z < None:
  for p in ps:
    z = p
    continue
    break
  

y = UxADT.C("Test", [UxADT.C("Ay", [x]), UxADT.C("Bay", [y])])
UxADT.C("Test", [a])
if __iml0 = uxadt.M(x, uxadt.C("Test", [uxadt.C("Ay", [uxadt.V("x")]), uxadt.C("Bay", [uxadt.V("y")])])):x = __iml0["x"];y = __iml0["y"];
  return x

while x in y:
  while x in y:
    x
  

x# Local variable.
for x:
  x# Local variable.
  x# Local variable.


def f (x, t, z):
  
  def g (x, y):
    Informl.plus(x, y)
  
  x
  UxADT.C("ImCon", [a, UxADT.C("ImCon", [UxADT.C("ImCon", [a, b, c]), UxADT.C("ImCon", [a, b, c]), c]), c])
  UxADT.C("ImA", [])
  z
  ifx:
    x
  
  x# Local variable.
  for x in y:
    x# Local variable.
    while x:
      while y:
        y# Local variable.
      
    
    x# Local variable.
    for x:
      x# Local variable.
    
    x# Local variable.
  
  x# Local variable.

x# Local variable.
x# Local variable.
