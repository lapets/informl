<?php
include "uxadt.php";
include "Informl.php";
define('_', null);
class Assignments{\uxadt\_(array('Impossible'=>array(),'What'=>array(),'Test'=>array(_,_),'Ay'=>array(_),'Bay'=>array(_),'Leaf'=>array(_),'Node'=>array(_,_),'ImCon'=>array(_,_,_),'ImA'=>array()));

  
  $x = 10;
  $y = 20;
  if (NULL) {
    return $x;
  }
  else if ($x != $y) {
    return $Impossible();
  }
  else {
    return $What();
  }
  $ps = array (1, 2, 3, 4);
  $p = indexOf($ps, 1);
  $z = 0;
  while ($z < NULL) {
    foreach ($ps as $p){

      $z = $p;
      continue;
      break;
    
}
  }
  $y = $Test($Ay($x), $Bay($y));
  $Test($a);
  if (_($x, Test(_,_))) {
    return $x;
  }
  while ($y as $x) {
    while ($y as $x) {
      $x;
    }
  }
  $x;
  for ($x) {
    $x;
    $x;
  }
  
  function count_leaves($tree) {$__iml0=$tree;
  return $__iml0
->_(Leaf(_), function($val) {
    return 1;
  })
->_(Node(_,_), function($a,$b) {
    return $informl->plus(count($a), count($b));
  })
->end;

    else {
      return 0;
    }
  
  }
  
  function f($x, $t, $z) {
    
    $g = function($x, $y) {
      $informl->plus($x, $y);
    }
    $x;
    $ImCon($a, $ImCon($ImCon($a, $b, $c), $ImCon($a, $b, $c), $c), $c);
    $ImA();
    $z;
    if ($x) {
      $x;
    }
    $x;
    foreach ($y as $x){

      $x;
      while ($x) {
        while ($y) {
          $y;
        }
      }
      $x;
      for ($x) {
        $x;
      }
      $x;
    
}
    $x;
  }
  $x;
  $x;
} ?>