
module Assignments

  local x := 10
  local y := 20
  
  if x >= y and x <= y
    return x
  elseif x != y
    return Impossible
  else
    return What

  local ps := [1,2,3,4]
  local p := indexOf(ps,1)
  local z := 0
  while z < x * y / 100^2
    for p in ps
      z := p
      continue
      break

  y := Test(Ay(x), Bay(y))
  Test(a)
 
  if x is Test(Ay(x), Bay(y))
    return x

  while x in y
    while x in y
      x

  local x

  for x
    local x
    local x
  
  function count_leaves(tree)
    if tree is Leaf(val)
      return 1
    elseif tree is Node(a,b)
      return count(a) + count(b)
    else
      return 0
  
  
  function f (x , t , z)
  
  
    function g (x,y)
      x + y
  
    x
    ImCon(a,ImCon(ImCon(a,b,c),ImCon(a,b,c),c),c)
    ImA
    z
  
    if x
      x
    local x
    for x in y
      local x
      while x
        while y
          local y
      local x
      for x
        local x
      local x
    local x

  local x
  local x

##eof