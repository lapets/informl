import uxadt
_ = None
import Informl
informl = Informl.Informl()
class Assignments:
  uxadt._({'Impossible':[],'What':[],'Test':[_,_],'Ay':[_],'Bay':[_],'Leaf':[_],'Node':[_,_],'ImCon':[_,_,_],'ImA':[]})


  x = 10# Local variable.
  y = 20# Local variable.
  if (x >= y and x <= y):
    return x
  
  elif x != y:
    return Impossible()
  
  else:
    return What()
  
  ps = [1, 2, 3, 4]# Local variable.
  p = Assignments.indexOf(ps, 1)# Local variable.
  z = 0# Local variable.
  while z < informl.div((x * y), (100**2)):
    for p in ps:
      z = p
      continue
      break
    
  
  y = Test(Ay(x), Bay(y))
  Test(a)
  if x < Test(Ay(_),Bay(_)):
    ((x),(y)) = x
    return x
  
  while x in y:
    while x in y:
      x
    
  
  x# Local variable.
  for x:
    x# Local variable.
    x# Local variable.
  
  
  def count_leaves (tree):
    if tree < Leaf(_):
      (val) = tree
      return 1
    
    elif tree < Node(_,_):
      (a,b) = tree
      return informl.plus(Assignments.count(a), Assignments.count(b))
    
    else:
      return 0
    
  
  
  def f (x, t, z):
    
    def g (x, y):
      informl.plus(x, y)
    
    x
    ImCon(a, ImCon(ImCon(a, b, c), ImCon(a, b, c), c), c)
    ImA()
    z
    if x:
      x
    
    x# Local variable.
    for x in y:
      x# Local variable.
      while x:
        while y:
          y# Local variable.
        
      
      x# Local variable.
      for x:
        x# Local variable.
      
      x# Local variable.
    
    x# Local variable.
  
  x# Local variable.
  x# Local variable.
